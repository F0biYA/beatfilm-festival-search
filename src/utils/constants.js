export const DECK_SIZE = window.innerWidth >= 1180 && 12;
export const TABLET_SIZE = window.innerWidth <= 1180 && window.innerWidth >= 481 && 8;
export const MOBILE_SIZE = window.innerWidth <= 480 && 5;
export const SHORT_DURATION = 40;
export const DECK_CARD = window.innerWidth >= 1180 && 3;
export const TABLET_CARD = window.innerWidth <= 1180 && window.innerWidth >= 481 && 2;
export const MOBILE_CARD = window.innerWidth <= 480 && 1;
export const API_URL = "https://api.putilin.student.nomoreparties.sbs";
